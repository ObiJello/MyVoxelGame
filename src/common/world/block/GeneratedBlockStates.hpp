// GENERATED by tools/gen_block_states.py — DO NOT EDIT BY HAND.
//
// Every block's BlockState property set, taken from vanilla rather than
// guessed. See the script header for why this is generated: MC's property
// VALUE ORDER decides the state index, and MC's property IDENTITY is
// (name, value-set) rather than name — `type` alone means three different
// things.
//
// Layout mirrors the other generated tables here (kBlockShapeTable,
// kWaterloggable): flat arrays with (begin, count) windows, keyed on the
// vanilla registry slug, which is column 2 of BlockDefs.inc.
//
// Properties are DEDUPED across blocks, exactly as vanilla shares one
// `BlockStateProperties.FACING` object between every block that uses it —
// which is what makes identity comparison valid.
//
// A block absent from kBlockStates has no properties and exactly one state.
#pragma once

#include <cstddef>
#include <cstdint>

namespace Game {

    // MC's three Property subclasses.
    enum class GeneratedPropertyKind : uint8_t { Bool = 0, Int = 1, Enum = 2 };

    struct GeneratedPropertyRow {
        const char* name;
        uint8_t     kind;         // GeneratedPropertyKind
        uint16_t    valueBegin;   // window into kPropertyValues
        uint16_t    valueCount;   // MC getPossibleValues().size(), always >= 2
    };

    struct GeneratedBlockStateRow {
        const char* slug;
        uint16_t    propBegin;    // window into kBlockPropertyRefs
        uint16_t    propCount;
        // Index of the block's default state within its OWN state list. NOT
        // always 0 — MC's any() takes the first value of every property and
        // BooleanProperty lists true first, so most blocks register a default
        // somewhere else entirely. 567 of these are non-zero.
        uint16_t    defaultIndex;
        // Product of the property value counts. Carried so the loader can
        // assert its own arithmetic against what vanilla allocated.
        uint16_t    stateCount;
    };

    // Value-name pool, shared by properties with identical value lists.
    extern const char* const            kPropertyValues[];
    extern const size_t                 kPropertyValueCount;

    // The 118 distinct (name, kind, values) properties.
    extern const GeneratedPropertyRow   kProperties[];
    extern const size_t                 kPropertyCount;

    // Flat property-index windows, one run per block, in MC's sorted-by-name
    // order — which is the order the state index is a mixed-radix number over,
    // with the LAST property varying fastest.
    extern const uint16_t               kBlockPropertyRefs[];
    extern const size_t                 kBlockPropertyRefCount;

    extern const GeneratedBlockStateRow kBlockStates[];
    extern const size_t                 kBlockStateRowCount;

    // MC BlockStateProperties constant names, verbatim. A property is
    // identified by (name, value-set), not by name — `type` alone is three
    // different properties — so the 13 reused names carry MC's own
    // disambiguating constant (SLAB_TYPE / CHEST_TYPE / PISTON_TYPE, …).
    enum class PropertyId : uint16_t {
        FACE = 0,                         // "face" = floor,wall,ceiling
        HORIZONTAL_FACING = 1,            // "facing" = north,south,west…
        POWERED = 2,                      // "powered" = true,false
        DOUBLE_BLOCK_HALF = 3,            // "half" = upper,lower
        HINGE = 4,                        // "hinge" = left,right
        OPEN = 5,                         // "open" = true,false
        EAST = 6,                         // "east" = true,false
        NORTH = 7,                        // "north" = true,false
        SOUTH = 8,                        // "south" = true,false
        WATERLOGGED = 9,                  // "waterlogged" = true,false
        WEST = 10,                        // "west" = true,false
        IN_WALL = 11,                     // "in_wall" = true,false
        ATTACHED = 12,                    // "attached" = true,false
        ROTATION = 13,                    // "rotation" = 0,1,2…
        DISTANCE = 14,                    // "distance" = 1,2,3…
        PERSISTENT = 15,                  // "persistent" = true,false
        AXIS = 16,                        // "axis" = x,y,z
        STAGE = 17,                       // "stage" = 0,1
        SIDE_CHAIN = 18,                  // "side_chain" = unconnected,right,center…
        SLAB_TYPE = 19,                   // "type" = top,bottom,double
        HALF = 20,                        // "half" = top,bottom
        STAIRS_SHAPE = 21,                // "shape" = straight,inner_left,inner_right…
        RAIL_SHAPE_STRAIGHT = 22,         // "shape" = north_south,east_west,ascending_east…
        FACING = 23,                      // "facing" = north,east,south…
        EAST_WALL = 24,                   // "east" = none,low,tall
        NORTH_WALL = 25,                  // "north" = none,low,tall
        SOUTH_WALL = 26,                  // "south" = none,low,tall
        UP = 27,                          // "up" = true,false
        WEST_WALL = 28,                   // "west" = none,low,tall
        AGE_1 = 29,                       // "age" = 0,1
        LEAVES = 30,                      // "leaves" = none,small,large
        HONEY_LEVEL = 31,                 // "honey_level" = 0,1,2…
        AGE_3 = 32,                       // "age" = 0,1,2…
        ATTACHMENT = 33,                  // "attachment" = floor,ceiling,single_wall…
        TILT = 34,                        // "tilt" = none,unstable,partial…
        OCCUPIED = 35,                    // "occupied" = true,false
        PART = 36,                        // "part" = head,foot
        CANDLES = 37,                     // "candles" = 1,2,3…
        LIT = 38,                         // "lit" = true,false
        HAS_BOTTLE_0 = 39,                // "has_bottle_0" = true,false
        HAS_BOTTLE_1 = 40,                // "has_bottle_1" = true,false
        HAS_BOTTLE_2 = 41,                // "has_bottle_2" = true,false
        DOWN = 42,                        // "down" = true,false
        DRAG = 43,                        // "drag" = true,false
        AGE_15 = 44,                      // "age" = 0,1,2…
        BITES = 45,                       // "bites" = 0,1,2…
        POWER = 46,                       // "power" = 0,1,2…
        SCULK_SENSOR_PHASE = 47,          // "sculk_sensor_phase" = inactive,active,cooldown
        SIGNAL_FIRE = 48,                 // "signal_fire" = true,false
        AGE_7 = 49,                       // "age" = 0,1,2…
        AGE_25 = 50,                      // "age" = 0,1,2…
        BERRIES = 51,                     // "berries" = true,false
        CONDITIONAL = 52,                 // "conditional" = true,false
        CHEST_TYPE = 53,                  // "type" = single,left,right
        SLOT_0_OCCUPIED = 54,             // "slot_0_occupied" = true,false
        SLOT_1_OCCUPIED = 55,             // "slot_1_occupied" = true,false
        SLOT_2_OCCUPIED = 56,             // "slot_2_occupied" = true,false
        SLOT_3_OCCUPIED = 57,             // "slot_3_occupied" = true,false
        SLOT_4_OCCUPIED = 58,             // "slot_4_occupied" = true,false
        SLOT_5_OCCUPIED = 59,             // "slot_5_occupied" = true,false
        AGE_5 = 60,                       // "age" = 0,1,2…
        AGE_2 = 61,                       // "age" = 0,1,2
        MODE_COMPARATOR = 62,             // "mode" = compare,subtract
        LEVEL_COMPOSTER = 63,             // "level" = 0,1,2…
        COPPER_GOLEM_POSE = 64,           // "copper_golem_pose" = standing,sitting,running…
        HANGING = 65,                     // "hanging" = true,false
        CRAFTING = 66,                    // "crafting" = true,false
        ORIENTATION = 67,                 // "orientation" = down_east,down_north,down_south…
        TRIGGERED = 68,                   // "triggered" = true,false
        CREAKING_HEART_STATE = 69,        // "creaking_heart_state" = uprooted,dormant,awake
        NATURAL = 70,                     // "natural" = true,false
        INVERTED = 71,                    // "inverted" = true,false
        CRACKED = 72,                     // "cracked" = true,false
        HYDRATION = 73,                   // "hydration" = 0,1,2…
        EYE = 74,                         // "eye" = true,false
        MOISTURE = 75,                    // "moisture" = 0,1,2…
        SNOWY = 76,                       // "snowy" = true,false
        ENABLED = 77,                     // "enabled" = true,false
        FACING_HOPPER = 78,               // "facing" = down,north,south…
        HAS_RECORD = 79,                  // "has_record" = true,false
        LEVEL = 80,                       // "level" = 0,1,2…
        SEGMENT_AMOUNT = 81,              // "segment_amount" = 1,2,3…
        HAS_BOOK = 82,                    // "has_book" = true,false
        AGE_4 = 83,                       // "age" = 0,1,2…
        PISTON_TYPE = 84,                 // "type" = normal,sticky
        HORIZONTAL_AXIS = 85,             // "axis" = x,z
        INSTRUMENT = 86,                  // "instrument" = harp,basedrum,snare…
        NOTE = 87,                        // "note" = 0,1,2…
        TIP = 88,                         // "tip" = true,false
        BOTTOM = 89,                      // "bottom" = true,false
        FLOWER_AMOUNT = 90,               // "flower_amount" = 1,2,3…
        EXTENDED = 91,                    // "extended" = true,false
        SHORT = 92,                       // "short" = true,false
        THICKNESS = 93,                   // "thickness" = tip_merge,tip,frustum…
        VERTICAL_DIRECTION = 94,          // "vertical_direction" = up,down
        LEVEL_CAULDRON = 95,              // "level" = 1,2,3
        RAIL_SHAPE = 96,                  // "shape" = north_south,east_west,ascending_east…
        EAST_REDSTONE = 97,               // "east" = up,side,none
        NORTH_REDSTONE = 98,              // "north" = up,side,none
        SOUTH_REDSTONE = 99,              // "south" = up,side,none
        WEST_REDSTONE = 100,              // "west" = up,side,none
        DELAY = 101,                      // "delay" = 1,2,3…
        LOCKED = 102,                     // "locked" = true,false
        CHARGES = 103,                    // "charges" = 0,1,2…
        STABILITY_DISTANCE = 104,         // "distance" = 0,1,2…
        BLOOM = 105,                      // "bloom" = true,false
        CAN_SUMMON = 106,                 // "can_summon" = true,false
        SHRIEKING = 107,                  // "shrieking" = true,false
        PICKLES = 108,                    // "pickles" = 1,2,3…
        HATCH = 109,                      // "hatch" = 0,1,2
        LAYERS = 110,                     // "layers" = 1,2,3…
        STRUCTUREBLOCK_MODE = 111,        // "mode" = save,load,corner…
        DUSTED = 112,                     // "dusted" = 0,1,2…
        TEST_BLOCK_MODE = 113,            // "mode" = start,log,fail…
        UNSTABLE = 114,                   // "unstable" = true,false
        OMINOUS = 115,                    // "ominous" = true,false
        TRIAL_SPAWNER_STATE = 116,        // "trial_spawner_state" = inactive,waiting_for_players,active…
        DISARMED = 117,                   // "disarmed" = true,false
        EGGS = 118,                       // "eggs" = 1,2,3…
        VAULT_STATE = 119,                // "vault_state" = inactive,active,unlocking…
        Count = 120
    };

    // Total distinct block states across every block this engine defines —
    // the size of the global state id space, and MC's
    // Block.BLOCK_STATE_REGISTRY.size(). Generated, never a literal: the
    // palette width is derived from it and must move when it does.
    inline constexpr uint32_t kBlockStateCount = 29613;
    inline constexpr int      kBlockStateBits  = 15;
    inline constexpr size_t   kStatefulBlockCount = 762;

    static_assert(kBlockStateCount <= (1u << kBlockStateBits),
                  "state id space does not fit the generated palette width");
    static_assert(kBlockStateCount > (1u << (kBlockStateBits - 1)),
                  "generated palette width is wider than the state space needs");

} // namespace Game
